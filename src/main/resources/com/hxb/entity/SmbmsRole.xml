<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hxb.dao.SmbmsRoleDao">
	<select id="getSmbmsRoleList" resultType="SmbmsRole">
		<!-- select * from smbms_role -->
		SELECT DISTINCT id,roleName FROM smbms_role
	</select>
</mapper>